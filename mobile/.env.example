# Copy to `.env` (Expo loads EXPO_PUBLIC_* into process.env at bundle time).
# These values are bundled into the app binary — never commit production tokens.
EXPO_PUBLIC_API_URL=http://127.0.0.1:8787
EXPO_PUBLIC_API_TOKEN=replace-with-server-BOTRADER_API_TOKEN
